//Write C code to print the square of all numbers from 1 to10.//
#include <stdio.h>
        int main () {
            int kare, sayac;
                 for(sayac=1; sayac<11; sayac++) {
                    kare = sayac * sayac;
                        printf("%d\n", kare);}
        return 0;
        }