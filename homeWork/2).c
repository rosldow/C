//Calculate the area of a circle with given radius//
#include <stdio.h>
    int main() {    
        int alan, yaricap,Pi=3;
            printf("Yaricapi giriniz: ");
            scanf("%d", &yaricap);
            alan = 2 * Pi * yaricap * yaricap;  
            printf("Cemberin alani = %d", alan);
    return 0;
    }