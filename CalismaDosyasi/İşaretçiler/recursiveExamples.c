#include <stdio.h>
/*
int usAlma (int x, int n) {
    if (n == 0 || x == 1) {
        return 1;
    }
    return x * usAlma(x , n-1);
}
int main () {
    int taban, us;
        printf("Lutfen taban sayiyi giriniz: ");
        scanf("%d", &taban);

        printf("Lutfen us degerini giriniz: ");
        scanf("%d", &us);

        printf("Girdiginiz sayilara gore sonuc: %d", usAlma(taban, us));
} */

/*
int fibo (int x) {
    if (x == 0) {
        return 0;
    }
    else if (x == 1) {
        return 1;
    }
    else {
        return fibo(x-1) + fibo(x-2);
    }    
}
int main () {
    int eleman, i;
        printf("Kacinci elemana kadar yazilmasini istiyorunuz? : ");
        scanf("%d", &eleman);

        printf("Fibonacci%d : ", eleman);
        
        for(i=0; i < eleman; i++) {
            printf("%d ", fibo(i));
        }
} */